spring.application.name=fourth

spring.datasource.url=jdbc:postgresql://localhost:5432/exam_attendance
spring.datasource.username=postgres
spring.datasource.password=e1n2o3c4h5
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true
spring.jpa.open-in-view=false

spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.validate-on-migrate=false
spring.flyway.clean-disabled=true

app.jwt.secret=change-this-to-a-strong-production-secret
app.jwt.access-token-expiration-ms=300000
app.jwt.refresh-token-expiration-ms=604800000

# Comma-separated browser origins allowed to call the API (CORS)
app.cors.allowed-origins=http://localhost:3000

# Examination slip QR tokens remain valid until this many hours after exam end_time
app.exam-slip.qr-validity-hours-after-end=6
